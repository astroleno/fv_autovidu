# fv_autovidu 依赖

# 图片处理（裁剪）
Pillow>=10.0.0

# HTTP 请求
requests>=2.28.0

# 配置
PyYAML>=6.0

# 环境变量（可选，用于 .env 加载 API Key）
python-dotenv>=1.0.0

# 后端 API（web/server）
fastapi>=0.115.0
uvicorn>=0.30.0
# Python 3.9 下 Pydantic 解析 PEP604 联合类型（如 X | None）时需此包；3.10+ 亦可保留无害
eval-type-backport>=0.2.0

# 剪映草稿：生成与官方兼容的 draft_content.json（时间轴）；见 pyJianYingDraft
# 依赖 pymediainfo；运行时尚需系统安装 libmediainfo（否则 can_parse 失败，见 jianying_service._ensure_mediainfo_for_jianying）
pyJianYingDraft>=0.2.6
