{
  "name": "fv-autovidu",
  "private": true,
  "scripts": {
    "dev": "concurrently -n api,web -c blue,green \"python -m uvicorn web.server.main:app --reload --port 8000\" \"pnpm --prefix web/frontend run dev\""
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
