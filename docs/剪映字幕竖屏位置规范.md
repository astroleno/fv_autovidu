# 剪映字幕竖屏位置规范（本仓库「规范版」）

**状态**：与 `jianying_text_track` 中 `position_mode=jianying_spec` 对齐  
**日期**：2026-03-31

---

## 1. 经验公式（像素 Y）

在剪映竖屏工作流中，字幕纵向位置可用下列经验关系（**n 为参与公式的行数**，**1 ≤ n ≤ 3**）：

\[
Y = -100n - 400
\]

- **Y**：与剪映草稿中 `clip.transform.y` 所用坐标系一致的**像素位移**方向约定（向下为负时，多行字幕更「低」对应更负的 Y）。
- **n**：由「换行分段数」经 **上限 3** 截断得到（见下文「行数怎么数」）；公式中 **不会出现 n>3**。

---

## 2. 与 pyJianYingDraft `transform_y` 的换算

[pyJianYingDraft](https://pypi.org/project/pyJianYingDraft/) 的 `ClipSettings` 约定：

> **垂直位移 `transform_y` 的单位为「半个画布高」。**

设画布高度为 \(H\) 像素（竖屏 1080p 一般为 **1920**，720p 竖屏一般为 **1280**），则：

\[
\text{transform\_y} = \frac{Y}{H/2} = \frac{2Y}{H}
\]

本仓库在导出时按**当前导出画布**的 `canvas_height` 做除法，因此同一公式在 720p / 1080p 下会自动随分辨率缩放（与「半画布高」定义一致）。

---

## 3. 行数 n 怎么取

**用于拆行计数的正文**来自 `subtitle_text_from_shot`（与导出一致）：译文 → 台词原文 → 结构化对白 → **画面描述**（`visualDescription`，当台词字段为空时）。

1. **换行分段数（原始）**：按 `\n` / `\r\n` 拆行，**忽略仅含空白的行**，非空行的条数；若没有任何非空行则按 **1**（与 `estimate_subtitle_line_count` 一致）。
2. **参与公式的 n**：`n = min(换行分段数, 3)`，实现为 `jianying_spec_line_count`（常量 `JIANYING_SPEC_MAX_LINES = 3`）。超过 3 段换行时，纵轴仍按 **n=3** 代入 \(Y=-100n-400\)（与「最多按三行排版」的产品约定一致）。
3. **自动换行**（仅剪映内折行、**文案里没有换行符**）时，整段视为 **1 行**，无法在导出侧按视觉行数推算；若要多行公式，请在分镜台词里**手动换行**。
4. **JSON 键名**：`episode.json` 若含 `dialogue_translation`、`visual_description` 等蛇形键，服务端 `Shot` 模型已用 Pydantic `AliasChoices` 兼容，避免整段台词被忽略。

---

## 4. 规范版字号（固定 13）

与行数 **无关**：`jianying_spec` 模式下每条字幕的 `TextStyle.size` 恒为 **13**（与产品/剪映侧约定一致）。

`manual` 模式下仍使用请求体中的统一 `subtitleFontSize`（4～16）。

---

## 5. API 行为

| `subtitlePositionMode` | 行为 |
|------------------------|------|
| `manual`（默认） | 全片字幕段使用同一 `subtitleTransformY`（-1.0～0.0）与同一 `subtitleFontSize`。 |
| `jianying_spec` | 每条字幕：`jianying_spec_line_count` 得 n（1～3）→ \(Y=-100n-400\) → `transform_y`；**字号固定 13**；**忽略**请求里的 `subtitleTransformY` 与 `subtitleFontSize`（请求体仍可传，兼容旧客户端）。 |

---

## 6. 示例（1080p 竖屏，H=1920）

| 行数 n | Y（像素） | transform_y（≈） |
|--------|-----------|------------------|
| 1 | -500 | -500/960 ≈ -0.521 |
| 2 | -600 | -600/960 = -0.625 |
| 3 | -700 | -700/960 ≈ -0.729 |

---

## 7. 参考

- `web/server/services/jianying_text_track.py`：`jianying_spec_y_pixel`、`jianying_spec_line_count`、`JIANYING_SPEC_FONT_SIZE`、`y_pixel_to_clip_transform_y`、`estimate_subtitle_line_count`
- `pyJianYingDraft.segment.ClipSettings` 文档字符串（`transform_y` 单位说明）
